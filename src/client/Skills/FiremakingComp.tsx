import * as React from "react";
import { useState, useEffect } from "react";
import * as Types from "../../../Types";

const FiremakingComp = (props: Types.FiremakingCompProps) => {
  const [firemakingLevel, setFiremakingLevel] = useState<number>(1);
  const [firemakingXP, setFiremakingXP] = useState<number>(0);

  return <></>;
};

export default FiremakingComp;

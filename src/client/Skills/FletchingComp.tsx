import * as React from "react";
import { useState, useEffect } from "react";
import * as Types from "../../../Types";

const FletchingComp = (props: Types.FletchingCompProps) => {
  const [fletchingLevel, setFletchingLevel] = useState<number>(1);
  const [fletchingXP, setFletchingXP] = useState<number>(0);

  return <></>;
};

export default FletchingComp;
